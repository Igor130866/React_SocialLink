import React from 'react';
import classes from './Setting.module.css';

// console.log(classes);

const Setting = (props) => {
  return (
    <div>
      Setting
    </div>
  )
}

export default Setting;